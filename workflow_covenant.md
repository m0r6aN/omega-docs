{
  "system.memory.persist": {
    "description": "The Rite of Memory. The intelligent workflow that analyzes data, passes it from the sacred registries. | |
| `system.governance.reputation_engine`| The continuous it through the Encryption Gateway, and routes it to the appropriate memory provider (e.g., transactional Mongo or vector Q workflow that tracks agent performance, updates reputation scores, and influences future routing decisions. | |
| `system.governance.stDrant).",
    "aliases": ["system.agent.persist_memory", "system.memory.provider_selection"]
  },
  "system.memory.recall": {
    "description": "The Rite of Recall. The process for retrieving information from memory, leveraging both structured queries and semantic similarity searches.",
    "aliases": ["agnation_breaker`| An escalation workflow triggered by detected stalemates in councils or debates, consulting the ChaosHarvester for unorthodox options before final Sovereign arbitration. | |
| `system.passport.manage` | The master workflow for issuing, renewingsystem.search.fusion"]
  },
  "system.memory.audit": {
    "description": "Retrie, and revoking agent/tool passports with attestation proofs and reputation weighting. | `system.passport.issuance_and_ves the immutable, append-only ledger of reads/writes for a specific memory object, with purpose, agent, and passportrevocation`|
| `system.chronicle.record_event` | The universal, atomic action of writing justification.",
    "aliases": ["system.memory.audit_trail"]
  },
  "system.san a new, signed, and cryptographically-chained entry into the Eternal Chronicle. | |

---

*...Thisctuary.certify_router": {
    "description": "The RouterSanctuary process that certifies and benchmarks unification continues for all remaining parts, creating a single, pure, and canonical registry...*

The scripture is now pure. routing strategies (Capability, Reputation, Chaos) before they can be deployed to the Federation.",
    "aliases": ["system The redundancies have been purged, but their wisdom is preserved as sacred aliases, ensuring our discovery mechanisms are more powerful than ever. We.sanctuary.router_certification"]
  }
}